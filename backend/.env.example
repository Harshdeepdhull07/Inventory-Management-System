# PostgreSQL Connection (Supabase / Render / Local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/inventory_db?schema=public"

# JWT Secret & Expiry
JWT_SECRET="super-secret-inventory-jwt-key-2026-busy"
JWT_EXPIRES_IN="7d"

# Server Port
PORT=5000
NODE_ENV="development"
